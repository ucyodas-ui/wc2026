pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp *.html public/
    - cp -r scripts public/
    - cp -r images public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"